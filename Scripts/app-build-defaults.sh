#!/usr/bin/env bash

# Keep the app bundle's default release identity in one place so packaging checks
# validate the current build contract instead of a stale historical release.
macchannel_default_version=1.2.6
macchannel_default_build_number=19
