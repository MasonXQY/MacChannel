# DropMesh 1.2.2

MacChannel 现在更名为 DropMesh，同时换上了全新的应用图标。这次更名不会改变你的设备身份、配对关系、设置、传输历史或接收目录，升级后无需重新配对。

本版还带来：

- 对方文件成功保存后发送 macOS 系统通知。
- 菜单栏图标显示绿色未读点，打开 DropMesh 后自动清除。
- 点击通知可在 Finder 中定位接收到的文件。
- 点击弹窗外部即可关闭传输、配对或设置界面，后台传输不会中断。

安装方式不变：打开 `DropMesh.dmg`，将 DropMesh 拖入“应用程序”并覆盖旧版即可。
